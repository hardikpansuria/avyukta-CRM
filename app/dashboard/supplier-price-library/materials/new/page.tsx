import { redirect } from "next/navigation"; import { verifyOrgSession } from "@/lib/auth/verify-org-session"; import { hasOrgPermission } from "@/lib/auth/permissions"; import { MaterialForm } from "../material-form"; export default async function NewMaterialPage({searchParams}:{searchParams:Promise<{duplicate?:string}>}){const session=await verifyOrgSession();if(!session)redirect("/login");if(!(await hasOrgPermission(session,"supplier_price_library","create")))redirect("/dashboard/access-denied?module=supplier_price_library");const{duplicate}=await searchParams;return <MaterialForm duplicateFrom={duplicate}/>}
