import { redirect } from "next/navigation"; import { verifyOrgSession } from "@/lib/auth/verify-org-session"; import { canEditSupplierPriceLibrary } from "@/lib/supplier-price-library/access"; import { MaterialForm } from "../material-form"; export default async function NewMaterialPage({searchParams}:{searchParams:Promise<{duplicate?:string}>}){const session=await verifyOrgSession();if(!session)redirect("/login");if(!canEditSupplierPriceLibrary(session.role))redirect("/dashboard/supplier-price-library/materials");const{duplicate}=await searchParams;return <MaterialForm duplicateFrom={duplicate}/>}
