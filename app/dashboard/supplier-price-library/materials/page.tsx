import { redirect } from "next/navigation"; import { verifyOrgSession } from "@/lib/auth/verify-org-session"; import { getSupplierPricePermissions } from "@/lib/auth/permissions"; import { MaterialsClient } from "./materials-client"; export default async function MaterialsPage(){const session=await verifyOrgSession();if(!session)redirect("/login");return <MaterialsClient permissions={await getSupplierPricePermissions(session)}/>}
