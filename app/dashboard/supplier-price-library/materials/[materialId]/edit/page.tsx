import { redirect } from "next/navigation"; import { verifyOrgSession } from "@/lib/auth/verify-org-session"; import { hasOrgPermission } from "@/lib/auth/permissions"; import { MaterialForm } from "../../material-form"; export default async function EditMaterialPage({params}:{params:Promise<{materialId:string}>}){const session=await verifyOrgSession();if(!session)redirect("/login");if(!(await hasOrgPermission(session,"supplier_price_library","edit")))redirect("/dashboard/access-denied?module=supplier_price_library");const{materialId}=await params;return <MaterialForm materialId={materialId}/>}
