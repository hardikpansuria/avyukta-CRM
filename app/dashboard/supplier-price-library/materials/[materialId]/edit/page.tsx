import { redirect } from "next/navigation"; import { verifyOrgSession } from "@/lib/auth/verify-org-session"; import { canEditSupplierPriceLibrary } from "@/lib/supplier-price-library/access"; import { MaterialForm } from "../../material-form"; export default async function EditMaterialPage({params}:{params:Promise<{materialId:string}>}){const session=await verifyOrgSession();if(!session)redirect("/login");if(!canEditSupplierPriceLibrary(session.role))redirect("/dashboard/supplier-price-library/materials");const{materialId}=await params;return <MaterialForm materialId={materialId}/>}
