import { createAdminClient } from "@/lib/supabase/admin"; import { isDuplicateError,jsonError,logDatabaseError,requireSupplierPriceSession,text,validUuid } from "@/lib/supplier-price-library/server";
type Body={category_id?:unknown;material_description?:unknown;size_specification?:unknown;grade_material_type?:unknown;unit_of_measure?:unknown;notes?:unknown};
export async function GET(_request:Request,context:RouteContext<"/api/org/supplier-price-library/materials/[materialId]">){const auth=await requireSupplierPriceSession("view");if("response"in auth)return auth.response;const{materialId}=await context.params;if(!validUuid(materialId))return jsonError("Material not found",404);const admin=createAdminClient();const {data,error}=await admin.from("supplier_price_materials").select("id,material_code,category_id,material_description,size_specification,grade_material_type,unit_of_measure,notes,is_archived,duplicated_from_material_id,created_at,updated_at").eq("org_id",auth.session.org_id).eq("id",materialId).maybeSingle();if(error)return jsonError("Unable to fetch material",500);if(!data)return jsonError("Material not found",404);const {data:category}=await admin.from("supplier_price_categories").select("id,category_name,is_archived").eq("org_id",auth.session.org_id).eq("id",data.category_id).maybeSingle();return Response.json({material:{...data,category}})}
export async function PATCH(request:Request,context:RouteContext<"/api/org/supplier-price-library/materials/[materialId]">){const auth=await requireSupplierPriceSession("edit");if("response"in auth)return auth.response;const{materialId}=await context.params;if(!validUuid(materialId))return jsonError("Material not found",404);let body:Body;try{body=await request.json()}catch{return jsonError("Invalid request body",400)}const categoryId=text(body.category_id,true),description=text(body.material_description,true),unit=text(body.unit_of_measure,true);if(!categoryId||!description||!unit)return jsonError("Category, material description, and unit of measure are required",400);const admin=createAdminClient();const[{data:material},{data:category}]=await Promise.all([admin.from("supplier_price_materials").select("id").eq("org_id",auth.session.org_id).eq("id",materialId).maybeSingle(),admin.from("supplier_price_categories").select("id,is_archived").eq("org_id",auth.session.org_id).eq("id",categoryId).maybeSingle()]);if(!material)return jsonError("Material not found",404);if(!category)return jsonError("Category not found",400);if(category.is_archived)return jsonError("Archived categories cannot be selected for materials",400);const {data,error}=await admin.from("supplier_price_materials").update({category_id:categoryId,material_description:description,size_specification:text(body.size_specification),grade_material_type:text(body.grade_material_type),unit_of_measure:unit,notes:text(body.notes),updated_by:auth.session.user.id,updated_at:new Date().toISOString()}).eq("org_id",auth.session.org_id).eq("id",materialId).select("id,material_code,category_id,material_description,size_specification,grade_material_type,unit_of_measure,notes,is_archived,duplicated_from_material_id,created_at,updated_at").maybeSingle();if(error){if(isDuplicateError(error))return jsonError("A matching material already exists in the Master Material Library.",409);logDatabaseError("Unable to update material",error);return jsonError("Unable to update material",500)}return Response.json({material:data})}
